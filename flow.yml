summary: ""
description: ""
value:
  modules:
    - id: a
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import os

          import json

          import csv

          import re

          import requests

          import pandas as pd

          from datetime import datetime

          from pymongo import MongoClient

          from io import StringIO

          from bs4 import BeautifulSoup

          import sqlalchemy as sa


          # ============================================

          # FONCTIONS DE CONNEXION AUX SOURCES

          # ============================================


          def read_csv_file(file_path):
              """Lit un fichier CSV"""
              data = []
              with open(file_path, 'r', encoding='utf-8') as f:
                  reader = csv.DictReader(f)
                  for row in reader:
                      data.append(row)
              return data

          def read_json_file(file_path):
              """Lit un fichier JSON"""
              with open(file_path, 'r', encoding='utf-8') as f:
                  return json.load(f)

          def read_xml_file(file_path):
              """Lit un fichier XML"""
              import xml.etree.ElementTree as ET

              def element_to_dict(element):
                  record = {}
                  for child in element:
                      if len(list(child)) > 0:
                          record[child.tag] = element_to_dict(child)
                      else:
                          record[child.tag] = child.text
                  return record

              tree = ET.parse(file_path)
              root = tree.getroot()
              children = list(root)

              if not children:
                  return {}

              if all(len(list(child)) > 0 for child in children):
                  return [element_to_dict(child) for child in children]

              if len(children) > 1 and all(len(list(child)) == 0 for child in children):
                  return [{child.tag: child.text for child in children}]

              first_child = children[0]
              if len(list(first_child)) > 0:
                  return [element_to_dict(first_child)]

              return {child.tag: child.text for child in children}

          def read_excel_file(file_path):
              """Lit un fichier Excel"""
              df = pd.read_excel(file_path)
              return df.to_dict('records') if not df.empty else []

          def read_parquet_file(file_path):
              """Lit un fichier Parquet"""
              df = pd.read_parquet(file_path)
              return df.to_dict('records') if not df.empty else []

          def read_html_file(file_path):
              """Lit un fichier HTML (scraping)"""
              with open(file_path, 'r', encoding='utf-8') as f:
                  soup = BeautifulSoup(f.read(), 'html.parser')
              
              data = {}
              
              titles = soup.find_all(['h1', 'h2', 'h3'])
              if titles:
                  data['titles'] = [t.get_text(strip=True) for t in titles[:5]]
              
              paragraphs = soup.find_all('p')
              if paragraphs:
                  data['paragraphs'] = [p.get_text(strip=True) for p in paragraphs[:5]]
              
              links = soup.find_all('a')
              if links:
                  data['links'] = [{'text': l.get_text(strip=True), 'href': l.get('href')} for l in links[:10]]
              
              tables = soup.find_all('table')
              if tables:
                  table_data = []
                  for table in tables[:2]:
                      rows = table.find_all('tr')
                      for row in rows:
                          cells = [c.get_text(strip=True) for c in row.find_all(['td', 'th'])]
                          if cells:
                              table_data.append(cells)
                  data['tables'] = table_data
              
              return data

          def read_sql_database(connection_string, query):
              """Lit depuis une base de données SQL"""
              engine = sa.create_engine(connection_string)
              with engine.connect() as conn:
                  df = pd.read_sql(query, conn)
              return df.to_dict('records') if not df.empty else []

          def fetch_api_rest(url, method='GET', headers=None, params=None,
          data=None):
              """Appelle une API REST"""
              if method.upper() == 'GET':
                  response = requests.get(url, headers=headers, params=params)
              elif method.upper() == 'POST':
                  response = requests.post(url, headers=headers, json=data)
              elif method.upper() == 'PUT':
                  response = requests.put(url, headers=headers, json=data)
              elif method.upper() == 'DELETE':
                  response = requests.delete(url, headers=headers)
              else:
                  raise ValueError(f"Méthode non supportée: {method}")
              
              response.raise_for_status()
              return response.json()

          def fetch_api_graphql(url, query, variables=None, headers=None):
              """Appelle une API GraphQL"""
              payload = {'query': query}
              if variables:
                  payload['variables'] = variables
              
              response = requests.post(url, json=payload, headers=headers)
              response.raise_for_status()
              return response.json()


          # ============================================

          # DÉTECTION DU FORMAT / SOURCE

          # ============================================


          def detect_format(file_path):
              """Détecte le format à partir de l'extension"""
              ext = os.path.splitext(file_path)[1].lower()
              formats = {
                  '.csv': 'csv',
                  '.json': 'json',
                  '.xml': 'xml',
                  '.xlsx': 'excel',
                  '.xls': 'excel',
                  '.parquet': 'parquet',
                  '.html': 'html',
                  '.htm': 'html'
              }
              return formats.get(ext, 'unknown')

          def detect_source_type(source_type, source_path):
              """Détecte le type de source"""
              if source_type:
                  return source_type
              if source_path.startswith('http://') or source_path.startswith('https://'):
                  return 'api'
              if source_path.startswith('postgresql://') or source_path.startswith('mysql://') or source_path.startswith('sqlite://'):
                  return 'sql'
              ext = os.path.splitext(source_path)[1].lower()
              if ext in ['.csv']:
                  return 'csv'
              if ext in ['.json']:
                  return 'json'
              if ext in ['.xml']:
                  return 'xml'
              if ext in ['.xlsx', '.xls']:
                  return 'excel'
              if ext in ['.parquet']:
                  return 'parquet'
              if ext in ['.html', '.htm']:
                  return 'html'
              return 'unknown'


          # ============================================

          # LECTURE DES DONNÉES SELON LA SOURCE

          # ============================================


          def read_data_from_source(source_type, source_path, file_path=None,
          format=None, **kwargs):
              """
              Lit les données selon le type de source
              """
              if source_type in ['csv', 'json', 'xml', 'excel', 'parquet', 'html']:
                  actual_path = file_path or source_path
                  if not os.path.exists(actual_path):
                      raise FileNotFoundError(f"Fichier introuvable: {actual_path}")
                  
                  if source_type == 'csv':
                      return read_csv_file(actual_path)
                  elif source_type == 'json':
                      return read_json_file(actual_path)
                  elif source_type == 'xml':
                      return read_xml_file(actual_path)
                  elif source_type == 'excel':
                      return read_excel_file(actual_path)
                  elif source_type == 'parquet':
                      return read_parquet_file(actual_path)
                  elif source_type == 'html':
                      return read_html_file(actual_path)
              
              elif source_type == 'api':
                  url = source_path
                  method = kwargs.get('method', 'GET')
                  headers = kwargs.get('headers', {})
                  data = kwargs.get('data', {})
                  params = kwargs.get('params', {})
                  return fetch_api_rest(url, method, headers, params, data)
              
              elif source_type == 'graphql':
                  url = source_path
                  query = kwargs.get('query', '')
                  variables = kwargs.get('variables', {})
                  headers = kwargs.get('headers', {})
                  return fetch_api_graphql(url, query, variables, headers)
              
              elif source_type == 'sql':
                  connection_string = source_path
                  query = kwargs.get('query', 'SELECT * FROM table LIMIT 1')
                  return read_sql_database(connection_string, query)
              
              else:
                  raise ValueError(f"Type de source non supporté: {source_type}")


          # ============================================

          # MÉTRIQUES (CPU / Mémoire)

          # ============================================


          def get_cpu_usage():
              try:
                  with open('/proc/stat', 'r') as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0

          def get_memory_mb():
              try:
                  with open('/proc/self/status', 'r') as f:
                      for line in f:
                          if line.startswith('VmRSS:'):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          # ============================================

          # SCRIPT PRINCIPAL

          # ============================================


          def main(
              source_type: str = None,
              source_path: str = None,
              file_path: str = None,
              format: str = None,
              method: str = 'GET',
              headers: str = None,
              params: str = None,
              data: str = None,
              query: str = None,
              variables: str = None
          ):
              """
              Ingestion - Connecteur vers toutes les sources de données
              """
              start_time = time.time()
              script_name = "ingestion"
              
              try:
                  actual_source_type = detect_source_type(source_type, source_path)
                  print(f"🔍 Type de source détecté: {actual_source_type}")
                  
                  actual_file_path = file_path
                  if actual_file_path is None and actual_source_type in ['csv', 'json', 'xml', 'excel', 'parquet', 'html']:
                      if source_path.startswith('/'):
                          actual_file_path = source_path
                      else:
                          actual_file_path = f"/data/{source_path}"
                  
                  headers_dict = json.loads(headers) if headers else {}
                  params_dict = json.loads(params) if params else {}
                  data_dict = json.loads(data) if data else {}
                  variables_dict = json.loads(variables) if variables else {}
                  
                  print(f"📁 Source: {source_path}")
                  print(f"📄 Type: {actual_source_type}")
                  
                  raw_data = read_data_from_source(
                      source_type=actual_source_type,
                      source_path=source_path,
                      file_path=actual_file_path,
                      format=format,
                      method=method,
                      headers=headers_dict,
                      params=params_dict,
                      data=data_dict,
                      query=query,
                      variables=variables_dict
                  )
                  
                  print(f"📊 Données lues: {json.dumps(raw_data, indent=2)}")
                  
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  
                  raw_payload = {
                      "source_type": actual_source_type,
                      "source_path": source_path,
                      "ingested_at": datetime.now().isoformat(),
                      "data": raw_data,
                      "file_format": format,
                      "method": method if actual_source_type == 'api' else None
                  }
                  
                  result = db.raw_data.insert_one({
                      "source_type": actual_source_type,
                      "source_path": source_path,
                      "ingested_at": datetime.now().isoformat(),
                      "step": script_name,
                      "raw_payload": raw_payload,
                      "status": "pending"
                  })
                  
                  duration_ms = (time.time() - start_time) * 1000
                  db.script_metrics.insert_one({
                      "script": script_name,
                      "duration_ms": duration_ms,
                      "status": "success",
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "timestamp": datetime.now().isoformat()
                  })
                  
                  return {
                      "status": "success",
                      "raw_payload": raw_payload,
                      "document_id": str(result.inserted_id),
                      "source_type": actual_source_type,
                      "source_path": source_path,
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "ingestion"
                  }
                  
              except FileNotFoundError as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one({
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "error",
                          "error": str(e)[:100],
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat()
                      })
                  except:
                      pass
                  
                  return {
                      "status": "error",
                      "message": str(e),
                      "step": "ingestion"
                  }
                  
              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one({
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "error",
                          "error": str(e)[:100],
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat()
                      })
                  except:
                      pass
                  
                  return {
                      "status": "error",
                      "message": str(e),
                      "step": "ingestion"
                  }
        language: python3
        input_transforms:
          data:
            expr: flow_input.data
            type: javascript
          query:
            expr: flow_input.query
            type: javascript
          format:
            expr: flow_input.format
            type: javascript
          method:
            expr: flow_input.method
            type: javascript
          params:
            expr: flow_input.params
            type: javascript
          headers:
            expr: flow_input.headers
            type: javascript
          file_path:
            expr: flow_input.file_path
            type: javascript
          variables:
            expr: flow_input.variables
            type: javascript
          source_path:
            expr: flow_input.source_path
            type: javascript
          source_type:
            expr: flow_input.source_type
            type: javascript
      summary: Ingestion
      continue_on_error: false
    - id: b
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import json

          import os

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def main(raw_data):
              """
              Profilage - Analyse des types, colonnes, valeurs nulles
              """
              start_time = time.time()
              script_name = "profiling"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans profiling:")
                  print(f"  - Type: {type(raw_data)}")

                  if isinstance(raw_data, dict):
                      print(f"  - Clés: {list(raw_data.keys())}")
                  elif isinstance(raw_data, list):
                      print(f"  - Longueur de la liste: {len(raw_data)}")
                      if len(raw_data) > 0:
                          print(f"  - Premier élément type: {type(raw_data[0])}")
                          if isinstance(raw_data[0], dict):
                              print(f"  - Clés du premier élément: {list(raw_data[0].keys())}")
                  print("=" * 60)

                  # Gestion des différents types de données
                  if isinstance(raw_data, list):
                      if len(raw_data) > 0 and all(isinstance(item, dict) for item in raw_data):
                          data = raw_data
                          print(f"📊 Liste de {len(raw_data)} éléments conservée pour profilage")
                      else:
                          data = {"items": raw_data, "count": len(raw_data)}
                  elif isinstance(raw_data, dict):
                      data = raw_data
                  else:
                      data = {"value": raw_data}

                  print(f"📊 Données à profiler: {json.dumps(data, indent=2)}")
                  print("=" * 60)

                  # Profilage
                  if isinstance(data, dict):
                      column_count = len(data)
                      data_types = {k: type(v).__name__ for k, v in data.items()}
                      null_values = {k: v is None for k, v in data.items()}
                      columns = list(data.keys())
                  elif isinstance(data, list):
                      columns = sorted({key for item in data if isinstance(item, dict) for key in item.keys()})
                      column_count = len(columns)
                      data_types = {}
                      null_values = {}
                      for column in columns:
                          first_value = next((item.get(column) for item in data if isinstance(item, dict) and item.get(column) is not None), None)
                          data_types[column] = type(first_value).__name__ if first_value is not None else "NoneType"
                          null_values[column] = any(isinstance(item, dict) and item.get(column) is None for item in data)
                  else:
                      column_count = 1
                      data_types = {"value_type": type(data).__name__}
                      null_values = {"is_null": data is None}
                      columns = ["value"]

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "success",
                      "profile": {
                          "column_count": column_count,
                          "data_types": data_types,
                          "null_values": null_values,
                          "columns": columns,
                      },
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "profiling",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "profiling"}
        language: python3
        input_transforms:
          raw_data:
            expr: results.a.raw_payload.data
            type: javascript
      summary: Profiling
    - id: c
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >
          import time

          import json

          import os

          from typing import Literal, Dict, Any

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def extract_data_from_html(html_data):
              """
              Extrait et formate les données HTML pour les rendre compatibles avec le mapping
              """
              records = []

              if "tables" in html_data and html_data["tables"]:
                  table_blob = html_data["tables"]

                  if (
                      table_blob
                      and isinstance(table_blob[0], list)
                      and table_blob[0]
                      and isinstance(table_blob[0][0], list)
                  ):
                      candidate_tables = table_blob
                  else:
                      candidate_tables = [table_blob]

                  for table in candidate_tables:
                      if not table or len(table) < 2:
                          continue

                      headers = table[0]
                      for values in table[1:]:
                          record = {}
                          for i, header in enumerate(headers):
                              if i >= len(values):
                                  continue

                              header_lower = header.lower().strip()
                              value = values[i]

                              if "id" in header_lower or "product" in header_lower:
                                  record["id"] = value
                              elif "name" in header_lower:
                                  record["name"] = value
                              elif "price" in header_lower or "amount" in header_lower:
                                  clean_price = (
                                      value.replace("$", "")
                                      .replace("€", "")
                                      .replace(",", "")
                                      .strip()
                                  )
                                  record["amount"] = clean_price
                              elif "date" in header_lower:
                                  record["date"] = value
                              elif "country" in header_lower or "origin" in header_lower:
                                  record["country"] = value
                              elif "email" in header_lower or "contact" in header_lower:
                                  record["email"] = value

                          if record:
                              records.append(record)

              if records:
                  return records

              result = {}

              if "titles" in html_data and html_data["titles"]:
                  result["title"] = html_data["titles"][0]

              if not result and "paragraphs" in html_data and html_data["paragraphs"]:
                  text = " ".join(html_data["paragraphs"])
                  import re

                  email_match = re.search(r"[\w\.-]+@[\w\.-]+\.\w+", text)
                  if email_match:
                      result["email"] = email_match.group()
                  price_match = re.search(r"\$?(\d+\.?\d*)", text)
                  if price_match:
                      result["amount"] = price_match.group(1)

              if "email" in result and "country" not in result:
                  if "@" in result["email"]:
                      domain = result["email"].split("@")[1].lower()
                      if "fr" in domain:
                          result["country"] = "FR"
                      elif "de" in domain:
                          result["country"] = "DE"
                      elif "uk" in domain or "co.uk" in domain:
                          result["country"] = "UK"
                      elif "ca" in domain:
                          result["country"] = "CA"
                      else:
                          result["country"] = "US"

              if "date" not in result:
                  from datetime import datetime

                  result["date"] = datetime.now().strftime("%Y-%m-%d")

              if "email" not in result:
                  result["email"] = "no-email@example.com"

              return result


          def main(raw_data, format: Literal["csv", "excel", "json", "html",
          "parquet"] = "json"):
              """
              Parsing - CSV, Excel, JSON, HTML, Parquet
              """
              start_time = time.time()
              script_name = "parsing"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans parsing:")
                  print(f"  - format: {format}")
                  print(f"  - raw_data type: {type(raw_data)}")

                  if isinstance(raw_data, dict):
                      print(f"  - raw_data keys: {list(raw_data.keys())}")
                  elif isinstance(raw_data, list):
                      print(f"  - raw_data length: {len(raw_data)}")
                      if len(raw_data) > 0:
                          print(f"  - Premier élément type: {type(raw_data[0])}")
                  else:
                      print(f"  - raw_data value: {raw_data}")
                  print("=" * 60)

                  # Gestion des différents types de données
                  if isinstance(raw_data, list):
                      if len(raw_data) == 0:
                          records = []
                      else:
                          records = raw_data
                          print(f"📊 Liste de {len(raw_data)} éléments conservée")
                  elif isinstance(raw_data, dict):
                      records = raw_data
                  else:
                      records = {"value": raw_data}

                  # Si c'est du HTML et que les clés caractéristiques sont présentes
                  if format == "html" or (
                      isinstance(records, dict)
                      and any(k in records for k in ["titles", "paragraphs", "tables"])
                  ):
                      print("🔄 Détection de données HTML, extraction en cours...")
                      records = extract_data_from_html(records)
                      print(f"📊 Données extraites: {json.dumps(records, indent=2)}")

                  print(f"📊 Records type: {type(records)}")
                  if isinstance(records, dict):
                      print(f"📊 Records keys: {list(records.keys())}")
                  elif isinstance(records, list):
                      print(f"📊 Records length: {len(records)}")
                  print("=" * 60)

                  parsed_data = {
                      "format": format,
                      "records": records,
                      "record_count": len(records) if isinstance(records, list) else 1,
                  }

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "success",
                      "parsed_data": parsed_data,
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "parsing",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "parsing"}
        language: python3
        input_transforms:
          format:
            expr: flow_input.format
            type: javascript
          raw_data:
            expr: results.a.raw_payload.data
            type: javascript
      summary: Parsing
    - id: d
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import json

          import os

          from datetime import datetime

          from pymongo import MongoClient


          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0

          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def normalize_records(records):
              if isinstance(records, list):
                  return [record if isinstance(record, dict) else {"value": record} for record in records], True
              if isinstance(records, dict):
                  return [records], False
              return [{"value": records}], False


          def map_record(record):
              mapped = {}

              if "id" in record:
                  mapped["user_id"] = record["id"]
              if "name" in record:
                  mapped["full_name"] = record["name"]
              if "amount" in record:
                  mapped["amount"] = record["amount"]
              elif "price" in record:
                  mapped["amount"] = record["price"]
              elif "salary" in record:
                  mapped["amount"] = record["salary"]
              if "date" in record:
                  mapped["transaction_date"] = record["date"]
              if "email" in record:
                  mapped["email_address"] = record["email"]

              country_value = record.get("country_code", record.get("country"))
              if country_value is not None:
                  mapped["country_code"] = country_value

              for key, value in record.items():
                  if key not in ["id", "name", "amount", "price", "salary", "date", "email", "country", "country_code"]:
                      mapped[key] = value

              return mapped

          def main(parsed_data: dict):
              """
              Mapping - champs source → modèle cible
              """
              start_time = time.time()
              script_name = "mapping"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans mapping:")
                  print(f"  - parsed_data keys: {list(parsed_data.keys()) if isinstance(parsed_data, dict) else 'Not a dict'}")
                  print("=" * 60)

                  # Récupération des records
                  if isinstance(parsed_data, dict) and "records" in parsed_data:
                      records = parsed_data["records"]
                      print(f"✅ Utilisation de parsed_data['records']")
                  else:
                      records = parsed_data
                      print(f"✅ Utilisation de parsed_data directement")

                  record_list, was_list = normalize_records(records)
                  if len(record_list) == 0:
                      return {
                          "status": "error",
                          "message": "Records list is empty",
                          "step": "mapping"
                      }

                  if not all(isinstance(record, dict) for record in record_list):
                      duration_ms = (time.time() - start_time) * 1000
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one({
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "error",
                          "error": "records is not a dict",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat()
                      })
                      return {
                          "status": "error",
                          "message": f"records is not a dict: {type(records)}",
                          "step": "mapping"
                      }

                  mapped_records = [map_record(record) for record in record_list]
                  mapped_data = mapped_records if was_list else mapped_records[0]

                  print(f"📊 Données mappées: {json.dumps(mapped_data, indent=2)}")
                  print("=" * 60)

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one({
                      "script": script_name,
                      "duration_ms": duration_ms,
                      "status": "success",
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "timestamp": datetime.now().isoformat()
                  })

                  return {
                      "status": "success",
                      "mapped_data": mapped_data,
                      "record_count": len(mapped_records),
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "mapping"
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one({
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "error",
                          "error": str(e)[:100],
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat()
                      })
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "mapping"}
        language: python3
        input_transforms:
          parsed_data:
            expr: results.c.parsed_data
            type: javascript
      summary: Mapping
    - id: e
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import json

          import os

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def normalize_records(data):
              if isinstance(data, list):
                  return [item if isinstance(item, dict) else {"value": item} for item in data], True
              if isinstance(data, dict):
                  return [data], False
              return [{"value": data}], False


          def clean_record(mapped_record):
              cleaned = {}
              cleaning_stats = {"trimmed": 0, "encoded": 0, "unchanged": 0}

              for key, value in mapped_record.items():
                  if isinstance(value, str):
                      trimmed = value.strip()
                      if trimmed != value:
                          cleaning_stats["trimmed"] += 1

                      encoded = trimmed.encode("utf-8").decode("utf-8")
                      if encoded != trimmed:
                          cleaning_stats["encoded"] += 1
                      else:
                          cleaning_stats["unchanged"] += 1

                      cleaned[key] = encoded
                  else:
                      cleaned[key] = value
                      cleaning_stats["unchanged"] += 1

              return cleaned, cleaning_stats


          def main(mapped_data: dict):
              """
              Nettoyage - trim, encodage, formats
              """
              start_time = time.time()
              script_name = "cleaning"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans cleaning:")
                  print(f"  - Type: {type(mapped_data)}")
                  print(
                      f"  - Clés: {list(mapped_data.keys()) if isinstance(mapped_data, dict) else 'Not a dict'}"
                  )
                  print("=" * 60)

                  record_list, was_list = normalize_records(mapped_data)

                  if len(record_list) == 0:
                      return {
                          "status": "error",
                          "message": "mapped_data is empty",
                          "step": "cleaning",
                      }

                  print(f"📊 Données avant nettoyage: {json.dumps(mapped_data, indent=2)}")
                  print("=" * 60)

                  cleaned_records = []
                  cleaning_stats = {"trimmed": 0, "encoded": 0, "unchanged": 0}

                  for record in record_list:
                      cleaned_record, record_stats = clean_record(record)
                      cleaned_records.append(cleaned_record)
                      for stat_key in cleaning_stats:
                          cleaning_stats[stat_key] += record_stats[stat_key]

                  cleaned = cleaned_records if was_list else cleaned_records[0]

                  print(f"📊 Statistiques de nettoyage: {json.dumps(cleaning_stats, indent=2)}")
                  print(f"📊 Données après nettoyage: {json.dumps(cleaned, indent=2)}")
                  print("=" * 60)

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "success",
                      "cleaned_data": cleaned,
                      "record_count": len(cleaned_records),
                      "cleaning_stats": cleaning_stats,
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "cleaning",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "cleaning"}
        language: python3
        input_transforms:
          mapped_data:
            expr: results.d.mapped_data
            type: javascript
      summary: Cleaning
    - id: f
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import json

          import os

          from typing import Literal, Dict, Any

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def normalize_records(data):
              if isinstance(data, list):
                  return [item if isinstance(item, dict) else {"value": item} for item in data], True
              if isinstance(data, dict):
                  return [data], False
              return [{"value": data}], False


          def type_record(cleaned_record, date_format):
              typed_record = {}
              typing_stats = {
                  "typed_as_date": 0,
                  "typed_as_number": 0,
                  "typed_as_bool": 0,
                  "typed_as_enum": 0,
                  "unchanged": 0,
                  "errors": 0,
              }

              enum_values = ["FR", "DE", "US", "UK", "CA", "ES", "IT", "JP", "BR", "AU"]

              for key, value in cleaned_record.items():
                  try:
                      if key in ["date", "transaction_date", "birth_date"] and isinstance(value, str):
                          try:
                              typed_record[key] = datetime.strptime(value, date_format).isoformat()
                              typing_stats["typed_as_date"] += 1
                          except ValueError:
                              typed_record[key] = value
                              typing_stats["unchanged"] += 1
                      elif key in ["amount", "price", "quantity", "value"]:
                          if isinstance(value, (int, float)):
                              typed_record[key] = float(value)
                              typing_stats["typed_as_number"] += 1
                          elif isinstance(value, str):
                              try:
                                  clean_value = value.replace("€", "").replace("$", "").replace(",", "").strip()
                                  typed_record[key] = float(clean_value)
                                  typing_stats["typed_as_number"] += 1
                              except ValueError:
                                  typed_record[key] = value
                                  typing_stats["unchanged"] += 1
                          else:
                              typed_record[key] = value
                              typing_stats["unchanged"] += 1
                      elif key in ["active", "enabled", "verified", "is_valid"]:
                          if isinstance(value, bool):
                              typed_record[key] = bool(value)
                              typing_stats["typed_as_bool"] += 1
                          elif isinstance(value, str):
                              if value.lower() in ["true", "yes", "1"]:
                                  typed_record[key] = True
                                  typing_stats["typed_as_bool"] += 1
                              elif value.lower() in ["false", "no", "0"]:
                                  typed_record[key] = False
                                  typing_stats["typed_as_bool"] += 1
                              else:
                                  typed_record[key] = value
                                  typing_stats["unchanged"] += 1
                          else:
                              typed_record[key] = value
                              typing_stats["unchanged"] += 1
                      elif key in ["country", "country_code", "status"]:
                          if isinstance(value, str) and value in enum_values:
                              typed_record[key] = value
                              typing_stats["typed_as_enum"] += 1
                          else:
                              typed_record[key] = value
                              typing_stats["unchanged"] += 1
                      else:
                          typed_record[key] = value
                          typing_stats["unchanged"] += 1
                  except Exception:
                      typing_stats["errors"] += 1
                      typed_record[key] = value

              return typed_record, typing_stats


          def main(cleaned_data: Dict[str, Any], date_format: str = "%Y-%m-%d"):
              """
              Typage - date, nombre, booléen, enum
              """
              start_time = time.time()
              script_name = "typing"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans typing:")
                  print(f"  - Type: {type(cleaned_data)}")
                  print(
                      f"  - Clés: {list(cleaned_data.keys()) if isinstance(cleaned_data, dict) else 'Not a dict'}"
                  )
                  print(f"  - date_format: {date_format}")
                  print("=" * 60)

                  record_list, was_list = normalize_records(cleaned_data)

                  if len(record_list) == 0:
                      return {
                          "status": "error",
                          "message": "cleaned_data is empty",
                          "step": "typing",
                      }

                  print(f"📊 Données avant typage: {json.dumps(cleaned_data, indent=2)}")
                  print("=" * 60)

                  typed_records = []
                  typing_stats = {
                      "typed_as_date": 0,
                      "typed_as_number": 0,
                      "typed_as_bool": 0,
                      "typed_as_enum": 0,
                      "unchanged": 0,
                      "errors": 0,
                  }
                  for record in record_list:
                      typed_record, record_stats = type_record(record, date_format)
                      typed_records.append(typed_record)
                      for stat_key in typing_stats:
                          typing_stats[stat_key] += record_stats[stat_key]

                  typed_data = typed_records if was_list else typed_records[0]

                  print("=" * 60)
                  print(f"📊 Statistiques de typage: {json.dumps(typing_stats, indent=2)}")
                  print(f"📊 Données après typage: {json.dumps(typed_data, indent=2)}")
                  print("=" * 60)

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "success" if typing_stats["errors"] == 0 else "partial",
                      "typed_data": typed_data,
                      "record_count": len(typed_records),
                      "typing_stats": typing_stats,
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "typing",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "typing"}
        language: python3
        input_transforms:
          date_format:
            type: static
            value: "%Y-%m-%d"
          cleaned_data:
            expr: results.e.cleaned_data
            type: javascript
      summary: Typing
    - id: g
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          import time

          import json

          import os

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def normalize_records(data):
              if isinstance(data, list):
                  return [item if isinstance(item, dict) else {"value": item} for item in data], True
              if isinstance(data, dict):
                  return [data], False
              return [{"value": data}], False


          def parse_amount(value):
              if isinstance(value, (int, float)):
                  return float(value)
              if isinstance(value, str):
                  try:
                      return float(value.replace("€", "").replace("$", "").replace(",", "").strip())
                  except ValueError:
                      return None
              return None


          def standardize_record(record, country_mapping):
              standardized = record.copy()
              record_stats = {
                  "countries_normalized": 0,
                  "currencies_converted": 0,
                  "units_normalized": 0,
              }

              country_value = standardized.get("country_code", standardized.get("country"))
              if isinstance(country_value, str):
                  country_code = country_value.upper()
                  if country_code in country_mapping:
                      standardized["country_code"] = country_code
                      standardized["country_name"] = country_mapping[country_code]
                      record_stats["countries_normalized"] += 1
                  elif "country" in standardized:
                      standardized["country"] = country_code

              if "amount" in standardized:
                  amount_numeric = parse_amount(standardized["amount"])
                  if amount_numeric is not None:
                      standardized["amount"] = amount_numeric
                      standardized["amount_usd"] = round(amount_numeric * 1.08, 2)
                      record_stats["currencies_converted"] += 1

              return standardized, record_stats


          def main(typed_data: dict):
              """
              Standardisation - noms, unités, devises, pays
              """
              start_time = time.time()
              script_name = "standardization"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans standardization:")
                  print(f"  - Type: {type(typed_data)}")
                  print(
                      f"  - Clés: {list(typed_data.keys()) if isinstance(typed_data, dict) else 'Not a dict'}"
                  )
                  print("=" * 60)

                  record_list, was_list = normalize_records(typed_data)

                  if len(record_list) == 0:
                      return {
                          "status": "error",
                          "message": "typed_data is empty",
                          "step": "standardization",
                      }

                  print(f"📊 Données avant standardisation: {json.dumps(typed_data, indent=2)}")
                  print("=" * 60)

                  std_stats = {
                      "countries_normalized": 0,
                      "currencies_converted": 0,
                      "units_normalized": 0,
                  }

                  country_mapping = {
                      "FR": "France",
                      "DE": "Germany",
                      "US": "United States",
                      "UK": "United Kingdom",
                      "CA": "Canada",
                      "ES": "Spain",
                      "IT": "Italy",
                      "JP": "Japan",
                      "BR": "Brazil",
                      "AU": "Australia",
                  }

                  standardized_records = []
                  for record in record_list:
                      standardized_record, record_stats = standardize_record(record, country_mapping)
                      standardized_records.append(standardized_record)
                      for stat_key in std_stats:
                          std_stats[stat_key] += record_stats[stat_key]

                  standardized = standardized_records if was_list else standardized_records[0]

                  print("=" * 60)
                  print(f"📊 Statistiques de standardisation: {json.dumps(std_stats, indent=2)}")
                  print(f"📊 Données après standardisation: {json.dumps(standardized, indent=2)}")
                  print("=" * 60)

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "success",
                      "standardized_data": standardized,
                      "record_count": len(standardized_records),
                      "standardization_stats": std_stats,
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "standardization",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "standardization"}
        language: python3
        input_transforms:
          typed_data:
            expr: results.f.typed_data
            type: javascript
      summary: Standardization
    - id: h
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: "import time\r

          import hashlib\r

          import json\r

          import os\r

          from datetime import datetime\r

          from pymongo import MongoClient\r

          \r

          \r

          def get_cpu_usage():\r

          \    try:\r

          \        with open(\"/proc/stat\", \"r\") as f:\r

          \            line = f.readline()\r

          \            parts = line.split()\r

          \            user = int(parts[1])\r

          \            nice = int(parts[2])\r

          \            system = int(parts[3])\r

          \            idle = int(parts[4])\r

          \            total = user + nice + system + idle\r

          \            return round(((total - idle) / total) * 100, 2) if total
          > 0 else 0\r

          \    except:\r

          \        return 0\r

          \r

          \r

          def get_memory_mb():\r

          \    try:\r

          \        with open(\"/proc/self/status\", \"r\") as f:\r

          \            for line in f:\r

          \                if line.startswith(\"VmRSS:\"):\r

          \                    kb = int(line.split()[1])\r

          \                    return round(kb / 1024, 2)\r

          \    except:\r

          \        return 0\r

          \r

          \r

          def normalize_records(data):\r

          \    if isinstance(data, list):\r

          \        return [\r

          \            item if isinstance(item, dict) else {\"value\": item} for
          item in data\r

          \        ], True\r

          \    if isinstance(data, dict):\r

          \        return [data], False\r

          \    return [{\"value\": data}], False\r

          \r

          \r

          def dedup_record(record):\r

          \    data_string = str(sorted(record.items()))\r

          \    timestamp = datetime.now().isoformat()\r

          \    unique_string = f\"{data_string}_{timestamp}_{id(record)}\"\r

          \    dedup_key = hashlib.md5(unique_string.encode()).hexdigest()\r

          \r

          \    record_with_key = record.copy()\r

          \    record_with_key[\"dedup_key\"] = dedup_key\r

          \    return record_with_key, dedup_key\r

          \r

          \r

          def main(standardized_data: dict):\r

          \    \"\"\"\r

          \    Déduplication - clé métier / hash\r

          \    \"\"\"\r

          \    start_time = time.time()\r

          \    script_name = \"dedup\"\r

          \r

          \    try:\r

          \        print(\"=\" * 60)\r

          \        print(\"📥 Données reçues dans dedup:\")\r

          \        print(f\"  - Type: {type(standardized_data)}\")\r

          \        print(\r

          \            f\"  - Clés: {list(standardized_data.keys()) if
          isinstance(standardized_data, dict) else 'Not a dict'}\"\r

          \        )\r

          \        print(\"=\" * 60)\r

          \r

          \        record_list, was_list =
          normalize_records(standardized_data)\r

          \r

          \        if len(record_list) == 0:\r

          \            return {\r

          \                \"status\": \"error\",\r

          \                \"message\": \"standardized_data is empty\",\r

          \                \"step\": \"dedup\",\r

          \            }\r

          \r

          \        deduplicated_records = []\r

          \        dedup_keys = []\r

          \        for record in record_list:\r

          \            record_with_key, dedup_key = dedup_record(record)\r

          \            deduplicated_records.append(record_with_key)\r

          \            dedup_keys.append(dedup_key)\r

          \r

          \        standardized_data_with_key = (\r

          \            deduplicated_records if was_list else
          deduplicated_records[0]\r

          \        )\r

          \r

          \        print(f\"🔑 Clé de déduplication générée:
          {dedup_keys[0]}\")\r

          \        print(\"=\" * 60)\r

          \r

          \        duration_ms = (time.time() - start_time) * 1000\r

          \        client =
          MongoClient(\"mongodb://admin:changeme@mongodb:27017/\")\r

          \        db = client[\"data_pipeline\"]\r

          \        db.script_metrics.insert_one(\r

          \            {\r

          \                \"script\": script_name,\r

          \                \"duration_ms\": duration_ms,\r

          \                \"status\": \"success\",\r

          \                \"cpu_percent\": get_cpu_usage(),\r

          \                \"memory_mb\": get_memory_mb(),\r

          \                \"timestamp\": datetime.now().isoformat(),\r

          \            }\r

          \        )\r

          \r

          \        return {\r

          \            \"status\": \"success\",\r

          \            \"deduplicated_data\": standardized_data_with_key,\r

          \            \"dedup_key\": dedup_keys[0],\r

          \            \"record_count\": len(deduplicated_records),\r

          \            \"duration_ms\": round(duration_ms, 2),\r

          \            \"cpu_percent\": get_cpu_usage(),\r

          \            \"memory_mb\": get_memory_mb(),\r

          \            \"step\": \"dedup\",\r

          \        }\r

          \r

          \    except Exception as e:\r

          \        duration_ms = (time.time() - start_time) * 1000\r

          \        try:\r

          \            client =
          MongoClient(\"mongodb://admin:changeme@mongodb:27017/\")\r

          \            db = client[\"data_pipeline\"]\r

          \            db.script_metrics.insert_one(\r

          \                {\r

          \                    \"script\": script_name,\r

          \                    \"duration_ms\": duration_ms,\r

          \                    \"status\": \"error\",\r

          \                    \"error\": str(e)[:100],\r

          \                    \"cpu_percent\": get_cpu_usage(),\r

          \                    \"memory_mb\": get_memory_mb(),\r

          \                    \"timestamp\": datetime.now().isoformat(),\r

          \                }\r

          \            )\r

          \        except:\r

          \            pass\r

          \        return {\"status\": \"error\", \"message\": str(e), \"step\":
          \"dedup\"}\r\n"
        language: python3
        input_transforms:
          standardized_data:
            expr: results.g.standardized_data
            type: javascript
      summary: Dedup
    - id: i
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >
          import time

          import json

          import os

          from datetime import datetime

          from pymongo import MongoClient



          def get_cpu_usage():
              try:
                  with open("/proc/stat", "r") as f:
                      line = f.readline()
                      parts = line.split()
                      user = int(parts[1])
                      nice = int(parts[2])
                      system = int(parts[3])
                      idle = int(parts[4])
                      total = user + nice + system + idle
                      return round(((total - idle) / total) * 100, 2) if total > 0 else 0
              except:
                  return 0


          def get_memory_mb():
              try:
                  with open("/proc/self/status", "r") as f:
                      for line in f:
                          if line.startswith("VmRSS:"):
                              kb = int(line.split()[1])
                              return round(kb / 1024, 2)
              except:
                  return 0


          def normalize_records(data):
              if isinstance(data, list):
                  return [
                      item if isinstance(item, dict) else {"value": item} for item in data
                  ], True
              if isinstance(data, dict):
                  return [data], False
              return [{"value": data}], False


          def validate_record(record):
              errors = []
              warnings = []

              if "user_id" not in record:
                  errors.append("Missing required field: user_id")

              if "email_address" not in record:
                  errors.append("Missing required field: email_address")

              if "amount" in record:
                  if record["amount"] < 0:
                      errors.append(f"Amount must be positive: {record['amount']}")
                  elif record["amount"] > 10000:
                      warnings.append(f"Amount is very high: {record['amount']}")

              if "email_address" in record:
                  email = record["email_address"]
                  if "@" not in email or "." not in email:
                      errors.append(f"Invalid email format: {email}")

              if "country_code" in record:
                  valid_countries = ["FR", "DE", "US", "UK", "CA", "ES", "IT", "JP", "BR", "AU"]
                  if record["country_code"] not in valid_countries:
                      warnings.append(f"Unknown country code: {record['country_code']}")

              return errors, warnings


          def main(deduplicated_data: dict):
              """
              Validation - règles métier + schéma
              """
              start_time = time.time()
              script_name = "validation"

              try:
                  print("=" * 60)
                  print("📥 Données reçues dans validation:")
                  print(f"  - Type: {type(deduplicated_data)}")
                  print(
                      f"  - Clés: {list(deduplicated_data.keys()) if isinstance(deduplicated_data, dict) else 'Not a dict'}"
                  )
                  print("=" * 60)

                  record_list, was_list = normalize_records(deduplicated_data)

                  if len(record_list) == 0:
                      return {
                          "status": "error",
                          "message": "deduplicated_data is empty",
                          "step": "validation",
                      }

                  all_errors = []
                  all_warnings = []
                  valid_records = []

                  for record in record_list:
                      record_errors, record_warnings = validate_record(record)
                      if not record_errors:
                          valid_records.append(record)
                      all_errors.extend(record_errors)
                      all_warnings.extend(record_warnings)

                  is_valid = len(all_errors) == 0
                  validated_output = (
                      valid_records if was_list else (valid_records[0] if valid_records else None)
                  )

                  print("=" * 60)
                  print(f"📊 Résultat de la validation:")
                  print(f"  - Statut: {'✅ VALIDE' if is_valid else '❌ REJETÉ'}")
                  print(f"  - Erreurs: {len(all_errors)}")
                  print(f"  - Avertissements: {len(all_warnings)}")
                  if all_errors:
                      print(f"  - Détails erreurs: {json.dumps(all_errors, indent=2)}")
                  if all_warnings:
                      print(f"  - Détails avertissements: {json.dumps(all_warnings, indent=2)}")
                  print("=" * 60)

                  duration_ms = (time.time() - start_time) * 1000
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                  db = client["data_pipeline"]
                  db.script_metrics.insert_one(
                      {
                          "script": script_name,
                          "duration_ms": duration_ms,
                          "status": "success",
                          "cpu_percent": get_cpu_usage(),
                          "memory_mb": get_memory_mb(),
                          "timestamp": datetime.now().isoformat(),
                      }
                  )

                  return {
                      "status": "valid" if is_valid else "rejected",
                      "validated_data": validated_output if is_valid else None,
                      "errors": all_errors,
                      "warnings": all_warnings,
                      "is_valid": is_valid,
                      "record_count": len(record_list),
                      "duration_ms": round(duration_ms, 2),
                      "cpu_percent": get_cpu_usage(),
                      "memory_mb": get_memory_mb(),
                      "step": "validation",
                  }

              except Exception as e:
                  duration_ms = (time.time() - start_time) * 1000
                  try:
                      client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                      db = client["data_pipeline"]
                      db.script_metrics.insert_one(
                          {
                              "script": script_name,
                              "duration_ms": duration_ms,
                              "status": "error",
                              "error": str(e)[:100],
                              "cpu_percent": get_cpu_usage(),
                              "memory_mb": get_memory_mb(),
                              "timestamp": datetime.now().isoformat(),
                          }
                      )
                  except:
                      pass
                  return {"status": "error", "message": str(e), "step": "validation"}
        language: python3
        input_transforms:
          deduplicated_data:
            expr: results.h.deduplicated_data
            type: javascript
      summary: Validation
    - id: j
      value:
        type: branchone
        default:
          - id: n
            value:
              lock: |-
                # workspace-dependencies-mode: manual
                # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
                # py: 3.12
                beautifulsoup4==4.12.2
                certifi==2026.6.17
                charset-normalizer==3.4.7
                dnspython==2.8.0
                et-xmlfile==2.0.0
                greenlet==3.5.3
                idna==3.18
                numpy==1.26.4
                openpyxl==3.1.2
                pandas==2.2.0
                psycopg2-binary==2.9.9
                pyarrow==14.0.1
                pymongo==4.6.1
                python-dateutil==2.8.2
                pytz==2026.2
                requests==2.31.0
                six==1.17.0
                soupsieve==2.8.4
                sqlalchemy==2.0.23
                typing-extensions==4.16.0
                tzdata==2026.2
                urllib3==2.7.0
              type: rawscript
              content: >-
                import time

                import json

                import os

                from datetime import datetime

                from pymongo import MongoClient



                def get_cpu_usage():
                    try:
                        with open("/proc/stat", "r") as f:
                            line = f.readline()
                            parts = line.split()
                            user = int(parts[1])
                            nice = int(parts[2])
                            system = int(parts[3])
                            idle = int(parts[4])
                            total = user + nice + system + idle
                            return round(((total - idle) / total) * 100, 2) if total > 0 else 0
                    except:
                        return 0


                def get_memory_mb():
                    try:
                        with open("/proc/self/status", "r") as f:
                            for line in f:
                                if line.startswith("VmRSS:"):
                                    kb = int(line.split()[1])
                                    return round(kb / 1024, 2)
                    except:
                        return 0


                def normalize_records(data):
                    if isinstance(data, list):
                        return [item if isinstance(item, dict) else {"value": item} for item in data], True
                    if isinstance(data, dict):
                        return [data], False
                    return [{"value": data}], False


                def enrich_record(record):
                    enriched = record.copy()
                    enriched.update(
                        {
                            "enriched_at": datetime.now().isoformat(),
                            "pipeline_version": "1.0.0",
                            "source_system": "windmill_pipeline",
                            "processing_timestamp": datetime.now().timestamp(),
                        }
                    )
                    return enriched


                def main(validated_data: dict):
                    """
                    Enrichissement - métadonnées, source, horodatage
                    """
                    start_time = time.time()
                    script_name = "enrichment"

                    try:
                        print("=" * 60)
                        print("📥 Données reçues dans enrichment:")
                        print(f"  - Type: {type(validated_data)}")
                        print(
                            f"  - Clés: {list(validated_data.keys()) if isinstance(validated_data, dict) else 'Not a dict'}"
                        )
                        print("=" * 60)

                        record_list, was_list = normalize_records(validated_data)

                        if len(record_list) == 0:
                            return {
                                "status": "error",
                                "message": "validated_data is empty",
                                "step": "enrichment",
                            }

                        enriched_records = [enrich_record(record) for record in record_list]
                        enriched = enriched_records if was_list else enriched_records[0]

                        print(f"📊 Données enrichies avec:")
                        if isinstance(enriched, list):
                            first_record = enriched[0] if enriched else {}
                            print(f"  - records: {len(enriched)}")
                            if first_record:
                                print(f"  - enriched_at: {first_record.get('enriched_at')}")
                                print(f"  - pipeline_version: {first_record.get('pipeline_version')}")
                                print(f"  - source_system: {first_record.get('source_system')}")
                        else:
                            print(f"  - enriched_at: {enriched['enriched_at']}")
                            print(f"  - pipeline_version: {enriched['pipeline_version']}")
                            print(f"  - source_system: {enriched['source_system']}")
                        print("=" * 60)

                        duration_ms = (time.time() - start_time) * 1000
                        try:
                            client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                            db = client["data_pipeline"]
                            db.script_metrics.insert_one(
                                {
                                    "script": script_name,
                                    "duration_ms": duration_ms,
                                    "status": "success",
                                    "cpu_percent": get_cpu_usage(),
                                    "memory_mb": get_memory_mb(),
                                    "timestamp": datetime.now().isoformat(),
                                }
                            )
                        except Exception:
                            pass

                        return {
                            "status": "success",
                            "enriched_data": enriched,
                            "record_count": len(enriched_records),
                            "duration_ms": round(duration_ms, 2),
                            "cpu_percent": get_cpu_usage(),
                            "memory_mb": get_memory_mb(),
                            "step": "enrichment",
                        }

                    except Exception as e:
                        duration_ms = (time.time() - start_time) * 1000
                        try:
                            client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                            db = client["data_pipeline"]
                            db.script_metrics.insert_one(
                                {
                                    "script": script_name,
                                    "duration_ms": duration_ms,
                                    "status": "error",
                                    "error": str(e)[:100],
                                    "cpu_percent": get_cpu_usage(),
                                    "memory_mb": get_memory_mb(),
                                    "timestamp": datetime.now().isoformat(),
                                }
                            )
                        except:
                            pass
                        return {"status": "error", "message": str(e), "step": "enrichment"}
              language: python3
              input_transforms:
                validated_data:
                  expr: results.i.validated_data
                  type: javascript
            summary: Enrichment
          - id: l
            value:
              lock: |-
                # workspace-dependencies-mode: manual
                # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
                # py: 3.12
                beautifulsoup4==4.12.2
                certifi==2026.6.17
                charset-normalizer==3.4.7
                dnspython==2.8.0
                et-xmlfile==2.0.0
                greenlet==3.5.3
                idna==3.18
                numpy==1.26.4
                openpyxl==3.1.2
                pandas==2.2.0
                psycopg2-binary==2.9.9
                pyarrow==14.0.1
                pymongo==4.6.1
                python-dateutil==2.8.2
                pytz==2026.2
                requests==2.31.0
                six==1.17.0
                soupsieve==2.8.4
                sqlalchemy==2.0.23
                typing-extensions==4.16.0
                tzdata==2026.2
                urllib3==2.7.0
              type: rawscript
              content: >-
                import time

                import json

                import os

                from typing import Literal, Dict, Any

                from datetime import datetime

                from pymongo import MongoClient


                def get_cpu_usage():
                    try:
                        with open('/proc/stat', 'r') as f:
                            line = f.readline()
                            parts = line.split()
                            user = int(parts[1])
                            nice = int(parts[2])
                            system = int(parts[3])
                            idle = int(parts[4])
                            total = user + nice + system + idle
                            return round(((total - idle) / total) * 100, 2) if total > 0 else 0
                    except:
                        return 0

                def get_memory_mb():
                    try:
                        with open('/proc/self/status', 'r') as f:
                            for line in f:
                                if line.startswith('VmRSS:'):
                                    kb = int(line.split()[1])
                                    return round(kb / 1024, 2)
                    except:
                        return 0


                def normalize_records(data):
                    if isinstance(data, list):
                        return [item if isinstance(item, dict) else {"data": item} for item in data], True
                    if isinstance(data, dict):
                        return [data], False
                    return [{"data": data}], False


                def write_document(collection, document):
                    document_to_write = document.copy() if isinstance(document, dict) else {"data": document}
                    document_to_write["written_at"] = datetime.now().isoformat()
                    document_to_write["_collection"] = collection.name

                    if "dedup_key" not in document_to_write or not document_to_write["dedup_key"]:
                        import hashlib
                        data_string = json.dumps(document_to_write, sort_keys=True, default=str)
                        unique_string = f"{data_string}_{datetime.now().isoformat()}"
                        document_to_write["dedup_key"] = hashlib.md5(unique_string.encode()).hexdigest()

                    existing = collection.find_one({"dedup_key": document_to_write["dedup_key"]})
                    if existing:
                        collection.update_one({"dedup_key": document_to_write["dedup_key"]}, {"$set": document_to_write})
                        return {"inserted_id": str(existing["_id"]), "updated": True, "dedup_key": document_to_write["dedup_key"]}

                    result = collection.insert_one(document_to_write)
                    return {"inserted_id": str(result.inserted_id), "updated": False, "dedup_key": document_to_write["dedup_key"]}

                def main(
                    data: Dict[str, Any],
                    collection: Literal["raw_data", "normalized_data", "rejected_data"] = "normalized_data"
                ):
                    """
                    Écriture dans MongoDB
                    """
                    start_time = time.time()
                    script_name = "mongodb_writer"
                    
                    try:
                        print("=" * 60)
                        print("📥 Données reçues dans mongodb_writer:")
                        print(f"  - collection: {collection}")
                        print(f"  - Type de data: {type(data)}")
                        print(f"  - Clés de data: {list(data.keys()) if isinstance(data, dict) else 'Not a dict'}")
                        print("=" * 60)
                        
                        record_list, was_list = normalize_records(data)

                        client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                        db = client["data_pipeline"]
                        coll = db[collection]

                        results = [write_document(coll, record) for record in record_list]
                        if len(results) == 1:
                            print(f"✅ Données insérées dans '{collection}'")
                            print(f"  - ID: {results[0]['inserted_id']}")
                            print(f"  - dedup_key: {results[0]['dedup_key']}")
                        else:
                            print(f"✅ {len(results)} documents traités dans '{collection}'")
                        print("=" * 60)
                        
                        duration_ms = (time.time() - start_time) * 1000
                        db.script_metrics.insert_one({
                            "script": script_name,
                            "duration_ms": duration_ms,
                            "status": "success",
                            "cpu_percent": get_cpu_usage(),
                            "memory_mb": get_memory_mb(),
                            "timestamp": datetime.now().isoformat()
                        })
                        
                        return {
                            "status": "success",
                            "inserted_id": results[0]["inserted_id"] if results else None,
                            "inserted_ids": [result["inserted_id"] for result in results],
                            "collection": collection,
                            "updated": results[0]["updated"] if results else False,
                            "dedup_key": results[0]["dedup_key"] if results else None,
                            "record_count": len(record_list),
                            "duration_ms": round(duration_ms, 2),
                            "cpu_percent": get_cpu_usage(),
                            "memory_mb": get_memory_mb(),
                            "step": "mongodb_write"
                        }
                        
                    except Exception as e:
                        duration_ms = (time.time() - start_time) * 1000
                        try:
                            client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                            db = client["data_pipeline"]
                            db.script_metrics.insert_one({
                                "script": script_name,
                                "duration_ms": duration_ms,
                                "status": "error",
                                "error": str(e)[:100],
                                "cpu_percent": get_cpu_usage(),
                                "memory_mb": get_memory_mb(),
                                "timestamp": datetime.now().isoformat()
                            })
                        except:
                            pass
                        return {
                            "status": "error",
                            "message": str(e),
                            "step": "mongodb_write"
                        }
              language: python3
              input_transforms:
                data:
                  expr: results.i.validated_data
                  type: javascript
                collection:
                  type: static
                  value: rejected_data
            summary: MongoDB_Writer
        branches:
          - expr: results.i.status == "valid"
            modules:
              - id: m
                value:
                  lock: |-
                    # workspace-dependencies-mode: manual
                    # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
                    # py: 3.12
                    beautifulsoup4==4.12.2
                    certifi==2026.6.17
                    charset-normalizer==3.4.7
                    dnspython==2.8.0
                    et-xmlfile==2.0.0
                    greenlet==3.5.3
                    idna==3.18
                    numpy==1.26.4
                    openpyxl==3.1.2
                    pandas==2.2.0
                    psycopg2-binary==2.9.9
                    pyarrow==14.0.1
                    pymongo==4.6.1
                    python-dateutil==2.8.2
                    pytz==2026.2
                    requests==2.31.0
                    six==1.17.0
                    soupsieve==2.8.4
                    sqlalchemy==2.0.23
                    typing-extensions==4.16.0
                    tzdata==2026.2
                    urllib3==2.7.0
                  type: rawscript
                  content: >-
                    import time

                    import json

                    import os

                    from datetime import datetime

                    from pymongo import MongoClient



                    def get_cpu_usage():
                        try:
                            with open("/proc/stat", "r") as f:
                                line = f.readline()
                                parts = line.split()
                                user = int(parts[1])
                                nice = int(parts[2])
                                system = int(parts[3])
                                idle = int(parts[4])
                                total = user + nice + system + idle
                                return round(((total - idle) / total) * 100, 2) if total > 0 else 0
                        except:
                            return 0


                    def get_memory_mb():
                        try:
                            with open("/proc/self/status", "r") as f:
                                for line in f:
                                    if line.startswith("VmRSS:"):
                                        kb = int(line.split()[1])
                                        return round(kb / 1024, 2)
                        except:
                            return 0


                    def normalize_records(data):
                        if isinstance(data, list):
                            return [item if isinstance(item, dict) else {"value": item} for item in data], True
                        if isinstance(data, dict):
                            return [data], False
                        return [{"value": data}], False


                    def enrich_record(record):
                        enriched = record.copy()
                        enriched.update(
                            {
                                "enriched_at": datetime.now().isoformat(),
                                "pipeline_version": "1.0.0",
                                "source_system": "windmill_pipeline",
                                "processing_timestamp": datetime.now().timestamp(),
                            }
                        )
                        return enriched


                    def main(validated_data: dict):
                        """
                        Enrichissement - métadonnées, source, horodatage
                        """
                        start_time = time.time()
                        script_name = "enrichment"

                        try:
                            print("=" * 60)
                            print("📥 Données reçues dans enrichment:")
                            print(f"  - Type: {type(validated_data)}")
                            print(
                                f"  - Clés: {list(validated_data.keys()) if isinstance(validated_data, dict) else 'Not a dict'}"
                            )
                            print("=" * 60)

                            record_list, was_list = normalize_records(validated_data)

                            if len(record_list) == 0:
                                return {
                                    "status": "error",
                                    "message": "validated_data is empty",
                                    "step": "enrichment",
                                }

                            enriched_records = [enrich_record(record) for record in record_list]
                            enriched = enriched_records if was_list else enriched_records[0]

                            print(f"📊 Données enrichies avec:")
                            if isinstance(enriched, list):
                                first_record = enriched[0] if enriched else {}
                                print(f"  - records: {len(enriched)}")
                                if first_record:
                                    print(f"  - enriched_at: {first_record.get('enriched_at')}")
                                    print(f"  - pipeline_version: {first_record.get('pipeline_version')}")
                                    print(f"  - source_system: {first_record.get('source_system')}")
                            else:
                                print(f"  - enriched_at: {enriched['enriched_at']}")
                                print(f"  - pipeline_version: {enriched['pipeline_version']}")
                                print(f"  - source_system: {enriched['source_system']}")
                            print("=" * 60)

                            duration_ms = (time.time() - start_time) * 1000
                            try:
                                client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                                db = client["data_pipeline"]
                                db.script_metrics.insert_one(
                                    {
                                        "script": script_name,
                                        "duration_ms": duration_ms,
                                        "status": "success",
                                        "cpu_percent": get_cpu_usage(),
                                        "memory_mb": get_memory_mb(),
                                        "timestamp": datetime.now().isoformat(),
                                    }
                                )
                            except Exception:
                                pass

                            return {
                                "status": "success",
                                "enriched_data": enriched,
                                "record_count": len(enriched_records),
                                "duration_ms": round(duration_ms, 2),
                                "cpu_percent": get_cpu_usage(),
                                "memory_mb": get_memory_mb(),
                                "step": "enrichment",
                            }

                        except Exception as e:
                            duration_ms = (time.time() - start_time) * 1000
                            try:
                                client = MongoClient("mongodb://admin:changeme@mongodb:27017/")
                                db = client["data_pipeline"]
                                db.script_metrics.insert_one(
                                    {
                                        "script": script_name,
                                        "duration_ms": duration_ms,
                                        "status": "error",
                                        "error": str(e)[:100],
                                        "cpu_percent": get_cpu_usage(),
                                        "memory_mb": get_memory_mb(),
                                        "timestamp": datetime.now().isoformat(),
                                    }
                                )
                            except:
                                pass
                            return {"status": "error", "message": str(e), "step": "enrichment"}
                  language: python3
                  input_transforms:
                    validated_data:
                      expr: results.i.validated_data
                      type: javascript
                summary: Enrichement
              - id: k
                value:
                  lock: |-
                    # workspace-dependencies-mode: manual
                    # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
                    # py: 3.12
                    beautifulsoup4==4.12.2
                    certifi==2026.6.17
                    charset-normalizer==3.4.7
                    dnspython==2.8.0
                    et-xmlfile==2.0.0
                    greenlet==3.5.3
                    idna==3.18
                    numpy==1.26.4
                    openpyxl==3.1.2
                    pandas==2.2.0
                    psycopg2-binary==2.9.9
                    pyarrow==14.0.1
                    pymongo==4.6.1
                    python-dateutil==2.8.2
                    pytz==2026.2
                    requests==2.31.0
                    six==1.17.0
                    soupsieve==2.8.4
                    sqlalchemy==2.0.23
                    typing-extensions==4.16.0
                    tzdata==2026.2
                    urllib3==2.7.0
                  type: rawscript
                  content: "import time\r

                    import json\r

                    import os\r

                    from typing import Literal, Dict, Any\r

                    from datetime import datetime\r

                    from pymongo import MongoClient\r

                    \r

                    def get_cpu_usage():\r

                    \    try:\r

                    \        with open('/proc/stat', 'r') as f:\r

                    \            line = f.readline()\r

                    \            parts = line.split()\r

                    \            user = int(parts[1])\r

                    \            nice = int(parts[2])\r

                    \            system = int(parts[3])\r

                    \            idle = int(parts[4])\r

                    \            total = user + nice + system + idle\r

                    \            return round(((total - idle) / total) * 100, 2)
                    if total > 0 else 0\r

                    \    except:\r

                    \        return 0\r

                    \r

                    def get_memory_mb():\r

                    \    try:\r

                    \        with open('/proc/self/status', 'r') as f:\r

                    \            for line in f:\r

                    \                if line.startswith('VmRSS:'):\r

                    \                    kb = int(line.split()[1])\r

                    \                    return round(kb / 1024, 2)\r

                    \    except:\r

                    \        return 0\r

                    \r

                    \r

                    def normalize_records(data):\r

                    \    if isinstance(data, list):\r

                    \        return [item if isinstance(item, dict) else
                    {\"data\": item} for item in data], True\r

                    \    if isinstance(data, dict):\r

                    \        return [data], False\r

                    \    return [{\"data\": data}], False\r

                    \r

                    \r

                    def write_document(collection, document):\r

                    \    document_to_write = document.copy() if
                    isinstance(document, dict) else {\"data\": document}\r

                    \    document_to_write[\"written_at\"] =
                    datetime.now().isoformat()\r

                    \    document_to_write[\"_collection\"] = collection.name\r

                    \r

                    \    if \"dedup_key\" not in document_to_write or not
                    document_to_write[\"dedup_key\"]:\r

                    \        import hashlib\r

                    \        data_string = json.dumps(document_to_write,
                    sort_keys=True, default=str)\r

                    \        unique_string =
                    f\"{data_string}_{datetime.now().isoformat()}\"\r

                    \        document_to_write[\"dedup_key\"] =
                    hashlib.md5(unique_string.encode()).hexdigest()\r

                    \r

                    \    existing = collection.find_one({\"dedup_key\":
                    document_to_write[\"dedup_key\"]})\r

                    \    if existing:\r

                    \        collection.update_one({\"dedup_key\":
                    document_to_write[\"dedup_key\"]}, {\"$set\":
                    document_to_write})\r

                    \        return {\"inserted_id\": str(existing[\"_id\"]),
                    \"updated\": True, \"dedup_key\":
                    document_to_write[\"dedup_key\"]}\r

                    \r

                    \    result = collection.insert_one(document_to_write)\r

                    \    return {\"inserted_id\": str(result.inserted_id),
                    \"updated\": False, \"dedup_key\":
                    document_to_write[\"dedup_key\"]}\r

                    \r

                    def main(\r

                    \    data: Dict[str, Any],\r

                    \    collection: Literal[\"raw_data\", \"normalized_data\",
                    \"rejected_data\"] = \"normalized_data\"\r

                    ):\r

                    \    \"\"\"\r

                    \    Écriture dans MongoDB\r

                    \    \"\"\"\r

                    \    start_time = time.time()\r

                    \    script_name = \"mongodb_writer\"\r

                    \    \r

                    \    try:\r

                    \        print(\"=\" * 60)\r

                    \        print(\"📥 Données reçues dans mongodb_writer:\")\r

                    \        print(f\"  - collection: {collection}\")\r

                    \        print(f\"  - Type de data: {type(data)}\")\r

                    \        print(f\"  - Clés de data: {list(data.keys()) if
                    isinstance(data, dict) else 'Not a dict'}\")\r

                    \        print(\"=\" * 60)\r

                    \        \r

                    \        record_list, was_list = normalize_records(data)\r

                    \r

                    \        client =
                    MongoClient(\"mongodb://admin:changeme@mongodb:27017/\")\r

                    \        db = client[\"data_pipeline\"]\r

                    \        coll = db[collection]\r

                    \r

                    \        results = [write_document(coll, record) for record
                    in record_list]\r

                    \        if len(results) == 1:\r

                    \            print(f\"✅ Données insérées dans
                    '{collection}'\")\r

                    \            print(f\"  - ID:
                    {results[0]['inserted_id']}\")\r

                    \            print(f\"  - dedup_key:
                    {results[0]['dedup_key']}\")\r

                    \        else:\r

                    \            print(f\"✅ {len(results)} documents traités
                    dans '{collection}'\")\r

                    \        print(\"=\" * 60)\r

                    \        \r

                    \        duration_ms = (time.time() - start_time) * 1000\r

                    \        db.script_metrics.insert_one({\r

                    \            \"script\": script_name,\r

                    \            \"duration_ms\": duration_ms,\r

                    \            \"status\": \"success\",\r

                    \            \"cpu_percent\": get_cpu_usage(),\r

                    \            \"memory_mb\": get_memory_mb(),\r

                    \            \"timestamp\": datetime.now().isoformat()\r

                    \        })\r

                    \        \r

                    \        return {\r

                    \            \"status\": \"success\",\r

                    \            \"inserted_id\": results[0][\"inserted_id\"] if
                    results else None,\r

                    \            \"inserted_ids\": [result[\"inserted_id\"] for
                    result in results],\r

                    \            \"collection\": collection,\r

                    \            \"updated\": results[0][\"updated\"] if results
                    else False,\r

                    \            \"dedup_key\": results[0][\"dedup_key\"] if
                    results else None,\r

                    \            \"record_count\": len(record_list),\r

                    \            \"duration_ms\": round(duration_ms, 2),\r

                    \            \"cpu_percent\": get_cpu_usage(),\r

                    \            \"memory_mb\": get_memory_mb(),\r

                    \            \"step\": \"mongodb_write\"\r

                    \        }\r

                    \        \r

                    \    except Exception as e:\r

                    \        duration_ms = (time.time() - start_time) * 1000\r

                    \        try:\r

                    \            client =
                    MongoClient(\"mongodb://admin:changeme@mongodb:27017/\")\r

                    \            db = client[\"data_pipeline\"]\r

                    \            db.script_metrics.insert_one({\r

                    \                \"script\": script_name,\r

                    \                \"duration_ms\": duration_ms,\r

                    \                \"status\": \"error\",\r

                    \                \"error\": str(e)[:100],\r

                    \                \"cpu_percent\": get_cpu_usage(),\r

                    \                \"memory_mb\": get_memory_mb(),\r

                    \                \"timestamp\": datetime.now().isoformat()\r

                    \            })\r

                    \        except:\r

                    \            pass\r

                    \        return {\r

                    \            \"status\": \"error\",\r

                    \            \"message\": str(e),\r

                    \            \"step\": \"mongodb_write\"\r

                    \        }"
                  language: python3
                  input_transforms:
                    data:
                      expr: results.i.validated_data
                      type: javascript
                    collection:
                      type: static
                      value: normalized_data
                summary: MongoDB_Writer
            summary: ""
            parallel: true
            skip_failure: true
      summary: ""
    - id: o
      value:
        lock: |-
          # workspace-dependencies-mode: manual
          # workspace-dependencies: default:38397b4a8bdee60b8f90b85df7160b61a65b6eef3837ee3c0a991d141e680782
          # py: 3.12
          beautifulsoup4==4.12.2
          certifi==2026.6.17
          charset-normalizer==3.4.7
          dnspython==2.8.0
          et-xmlfile==2.0.0
          greenlet==3.5.3
          idna==3.18
          numpy==1.26.4
          openpyxl==3.1.2
          pandas==2.2.0
          psycopg2-binary==2.9.9
          pyarrow==14.0.1
          pymongo==4.6.1
          python-dateutil==2.8.2
          pytz==2026.2
          requests==2.31.0
          six==1.17.0
          soupsieve==2.8.4
          sqlalchemy==2.0.23
          typing-extensions==4.16.0
          tzdata==2026.2
          urllib3==2.7.0
        type: rawscript
        content: >-
          """

          Envoi des métriques du pipeline à l'OTEL Collector

          Métriques : Comptage, Latence, Erreurs, CPU, Mémoire

          """

          import time

          import json

          import urllib.request

          import urllib.error

          from datetime import datetime

          from pymongo import MongoClient


          OTEL_COLLECTOR_URL = "http://otel_collector:4318/v1/metrics"



          def count_payload_records(payload):
              if isinstance(payload, list):
                  return len(payload)
              if isinstance(payload, dict):
                  return 1
              if payload is None:
                  return 0
              return 1


          def count_raw_records(db):
              total = 0
              for document in db.raw_data.find({}, {"raw_payload.data": 1}):
                  raw_data = document.get("raw_payload", {}).get("data")
                  total += count_payload_records(raw_data)
              return total


          def count_raw_records_by_source(db):
              counts = {}
              for document in db.raw_data.find({}, {"raw_payload.data": 1, "source_type": 1}):
                  source_type = document.get("source_type") or "unknown"
                  raw_data = document.get("raw_payload", {}).get("data")
                  counts[source_type] = counts.get(source_type, 0) + count_payload_records(raw_data)
              return counts

          def send_metric(name, value, unit="1", description="",
          attributes=None):
              """Envoie une métrique à l'OTEL Collector"""
              if attributes is None:
                  attributes = {}
              
              payload = {
                  "resourceMetrics": [{
                      "resource": {
                          "attributes": [
                              {"key": "service.name", "value": {"stringValue": "windmill-pipeline"}},
                              {"key": "service.version", "value": {"stringValue": "1.0.0"}}
                          ]
                      },
                      "scopeMetrics": [{
                          "scope": {"name": "pipeline.metrics", "version": "1.0.0"},
                          "metrics": [{
                              "name": name,
                              "description": description,
                              "unit": unit,
                              "gauge": {
                                  "dataPoints": [{
                                      "attributes": [
                                          {"key": k, "value": {"stringValue": str(v)}} 
                                          for k, v in attributes.items()
                                      ],
                                      "timeUnixNano": str(int(time.time() * 1e9)),
                                      "asDouble": float(value)
                                  }]
                              }
                          }]
                      }]
                  }]
              }
              
              try:
                  data = json.dumps(payload).encode('utf-8')
                  req = urllib.request.Request(
                      OTEL_COLLECTOR_URL,
                      data=data,
                      headers={'Content-Type': 'application/json'},
                      method='POST'
                  )
                  with urllib.request.urlopen(req, timeout=5) as response:
                      if response.status == 200:
                          print(f"✅ Métrique envoyée: {name}={value}")
                          return True
                      else:
                          print(f"⚠️ Erreur envoi {name}: {response.status}")
                          return False
              except Exception as e:
                  print(f"⚠️ Erreur envoi métrique {name}: {e}")
                  return False


          def main():
              """Met à jour toutes les métriques du pipeline"""
              
              try:
                  client = MongoClient("mongodb://admin:changeme@mongodb:27017/", serverSelectionTimeoutMS=5000)
                  db = client["data_pipeline"]
                  
                  # ============================================
                  # 1. MÉTRIQUES DE COMPTAGE
                  # ============================================
                  raw = count_raw_records(db)
                  norm = db.normalized_data.count_documents({})
                  rej = db.rejected_data.count_documents({})
                  pending = db.raw_data.count_documents({"status": "pending"})
                  processing = db.raw_data.count_documents({"status": "processing"})
                  
                  sources = count_raw_records_by_source(db)
                  
                  print(f"📊 Métriques de comptage: raw={raw}, normalized={norm}, rejected={rej}")
                  
                  send_metric("pipeline.raw.data.total", raw, "1", "Total raw data", {"type": "raw"})
                  send_metric("pipeline.normalized.data.total", norm, "1", "Total normalized data", {"type": "normalized"})
                  send_metric("pipeline.rejected.data.total", rej, "1", "Total rejected data", {"type": "rejected"})
                  send_metric("pipeline.raw.pending.total", pending, "1", "Pending data", {"status": "pending"})
                  send_metric("pipeline.raw.processing.total", processing, "1", "Processing data", {"status": "processing"})
                  
                  for source_type, count in sources.items():
                      send_metric("pipeline.raw.by.source", count, "1", "Data by source", {"source_type": source_type})
                  
                  # ============================================
                  # 2. MÉTRIQUES DE LATENCE PAR TÂCHE
                  # ============================================
                  print("\n⏱️ Métriques de latence:")
                  scripts = ["ingestion", "profiling", "parsing", "mapping", "cleaning", 
                             "typing", "standardization", "dedup", "validation", "enrichment", "mongodb_writer"]
                  
                  for script in scripts:
                      last = db.script_metrics.find_one(
                          {"script": script},
                          sort=[("timestamp", -1)]
                      )
                      if last:
                          duration = last.get("duration_ms", 0)
                          status = last.get("status", "unknown")
                          send_metric(
                              "pipeline.latency.last",
                              duration,
                              "ms",
                              f"Last execution duration for {script}",
                              {"script": script, "status": status}
                          )
                      
                      # Durée moyenne
                      avg_docs = list(db.script_metrics.find(
                          {"script": script}
                      ).sort("timestamp", -1).limit(10))
                      
                      if avg_docs:
                          avg_duration = sum(d.get("duration_ms", 0) for d in avg_docs) / len(avg_docs)
                          send_metric(
                              "pipeline.latency.avg",
                              round(avg_duration, 2),
                              "ms",
                              f"Average execution duration for {script}",
                              {"script": script}
                          )
                  
                  # ============================================
                  # 3. MÉTRIQUES D'ERREURS PAR TÂCHE
                  # ============================================
                  print("\n❌ Métriques d'erreurs:")
                  
                  for script in scripts:
                      errors = db.script_metrics.count_documents({
                          "script": script,
                          "status": "error"
                      })
                      successes = db.script_metrics.count_documents({
                          "script": script,
                          "status": "success"
                      })
                      
                      send_metric(
                          "pipeline.errors.total",
                          errors,
                          "1",
                          f"Total errors for {script}",
                          {"script": script}
                      )
                      send_metric(
                          "pipeline.success.total",
                          successes,
                          "1",
                          f"Total successes for {script}",
                          {"script": script}
                      )
                      
                      total = errors + successes
                      if total > 0:
                          error_rate = (errors / total) * 100
                          send_metric(
                              "pipeline.error.rate",
                              round(error_rate, 2),
                              "%",
                              f"Error rate for {script}",
                              {"script": script}
                          )
                  
                  # ============================================
                  # 4. MÉTRIQUES CPU PAR TÂCHE
                  # ============================================
                  print("\n💻 Métriques CPU par tâche:")
                  
                  for script in scripts:
                      last_cpu = db.script_metrics.find_one(
                          {"script": script},
                          sort=[("timestamp", -1)]
                      )
                      if last_cpu and "cpu_percent" in last_cpu:
                          send_metric(
                              "pipeline.cpu.percent",
                              last_cpu["cpu_percent"],
                              "%",
                              f"CPU usage for {script}",
                              {"script": script}
                          )
                      
                      # CPU moyenne
                      avg_cpu_docs = list(db.script_metrics.find(
                          {"script": script, "cpu_percent": {"$exists": True}}
                      ).sort("timestamp", -1).limit(10))
                      
                      if avg_cpu_docs:
                          avg_cpu = sum(d.get("cpu_percent", 0) for d in avg_cpu_docs) / len(avg_cpu_docs)
                          send_metric(
                              "pipeline.cpu.avg",
                              round(avg_cpu, 2),
                              "%",
                              f"Average CPU usage for {script}",
                              {"script": script}
                          )
                  
                  # ============================================
                  # 5. MÉTRIQUES MÉMOIRE PAR TÂCHE
                  # ============================================
                  print("\n💾 Métriques Mémoire par tâche:")
                  
                  for script in scripts:
                      last_memory = db.script_metrics.find_one(
                          {"script": script},
                          sort=[("timestamp", -1)]
                      )
                      if last_memory and "memory_mb" in last_memory:
                          send_metric(
                              "pipeline.memory.mb",
                              last_memory["memory_mb"],
                              "MB",
                              f"Memory usage for {script}",
                              {"script": script}
                          )
                      
                      # Mémoire moyenne
                      avg_memory_docs = list(db.script_metrics.find(
                          {"script": script, "memory_mb": {"$exists": True}}
                      ).sort("timestamp", -1).limit(10))
                      
                      if avg_memory_docs:
                          avg_memory = sum(d.get("memory_mb", 0) for d in avg_memory_docs) / len(avg_memory_docs)
                          send_metric(
                              "pipeline.memory.avg",
                              round(avg_memory, 2),
                              "MB",
                              f"Average memory usage for {script}",
                              {"script": script}
                          )
                  
                  print("\n✅ Toutes les métriques envoyées avec succès !")
                  return {"status": "success", "step": "send_metrics"}
                  
              except Exception as e:
                  print(f"❌ Erreur: {e}")
                  return {"status": "error", "message": str(e), "step": "send_metrics"}
        language: python3
        input_transforms: {}
      summary: Metrics
  flow_env: {}
  groups: []
schema:
  type: object
  properties:
    source_type:
      type: string
      description: Type de source de données
      enum:
        - csv
        - json
        - xml
        - excel
        - parquet
        - html
        - api
        - graphql
        - sql
      default: csv
    source_path:
      type: string
      description: Chemin du fichier, URL de l'API, ou chaîne de connexion SQL
      default: /data/sales_2024.csv
    method:
      type: string
      description: Méthode HTTP pour API REST
      enum:
        - GET
        - POST
        - PUT
        - DELETE
      default: GET
    headers:
      type: string
      description: Headers HTTP (format JSON)
      default: "{}"
    params:
      type: string
      description: Paramètres de requête (format JSON)
      default: "{}"
    data:
      type: string
      description: Corps de la requête (format JSON) pour API POST/PUT
      default: "{}"
    query:
      type: string
      description: Requête GraphQL ou SQL
      default: ""
    variables:
      type: string
      description: Variables GraphQL (format JSON)
      default: "{}"
  required:
    - source_type
    - source_path
  order:
    - source_type
    - source_path
    - method
    - headers
    - params
    - data
    - query
    - variables
