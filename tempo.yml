server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:55680

storage:
  trace:
    backend: local
    local:
      path: /var/tempo/blocks